services:
  whisper-api:
    image: ghcr.io/the80hz/whisper-api:gpu
    build:
      args:
        INSTALL_GPU: "true"
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              count: all
