services:
  whisper-api:
    build:
      args:
        INSTALL_GPU: "true"
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
              count: all
